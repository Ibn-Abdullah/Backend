{
	"name": "project2",
	"version": "1.0.0",
	"description": "A Node.js To-do app with Express, SQLite, and JWT authentication",
	"main": "src/server.js",
	"scripts": {
		"dev": "nodemon --env-file=.env --experimental-sqlite ./src/server.js",
		"start": "node --env-file=.env --experimental-sqlite ./src/server.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"type": "module",
	"dependencies": {
		"bcryptjs": "^3.0.2",
		"express": "^5.1.0",
		"jsonwebtoken": "^9.0.2",
		"sql-formatter": "^15.6.6"
	},
	"devDependencies": {
		"nodemon": "^3.1.10"
	}
}
